export interface Products {
    name: string | null;
    id: string | null;
    price: string | null
}

export interface newProducts {
    name: string | null;
    id: string | null;
    price: string | null;
}